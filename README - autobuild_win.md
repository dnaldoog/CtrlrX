
To run **autobuild_win.bat** you need Ninja installed:

To check whther you have Ninja, rn:

```cmd
ninja --version
```

If it's not installed, the easiest way to get it is via Visual Studio Installer (it's bundled under the C++ CMake tools component) or via winget:
```cmd
winget install Ninja-build.Ninja
```